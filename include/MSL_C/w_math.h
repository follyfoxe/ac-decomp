#ifndef W_MATH_H
#define W_MATH_H

#include <float.h>
#include <math.h>

#define __abs(x) abs(x)
#define __frsqrte(x) (1.0 / sqrtf(x))

#define fatan2(y, x) atan2(y, x)
#define facos(x) (f32)acos(x)
#define fsqrt(x) sqrtf(x)

#endif
